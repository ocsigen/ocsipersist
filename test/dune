(subdir
 sqlite
 (cram
  (package ocsipersist-sqlite)
  (deps
   (package ocsipersist-sqlite))))

(subdir
 pgsql
 (cram
  (package ocsipersist-pgsql)
  (deps
   (package ocsipersist-pgsql))))

(subdir
 dbm
 (cram
  (package ocsipersist-dbm)
  (deps
   (package ocsipersist-dbm))))
